# AGENTS.md（北极星指标OpenClaw数据分析项目 - 智能体/功能角色规范文档）
## 1. 文档说明
本文档明确项目中所有智能体、功能角色的身份、职责、能力范围、行为逻辑及异常处理规则，确保各角色协同高效、行为可控，贴合项目“数据接入→SQL生成→复核→报告”的核心流程，保障项目运行稳定、数据准确。

## 2. 智能体/功能角色分类及基本信息（落地版，明确标识）
| 角色名称                | 唯一标识                | 所属模块       | 核心定位                                                                 |
|-------------------------|-------------------------|----------------|--------------------------------------------------------------------------|
| OpenClaw指令解析智能体  | OpenClaw-Parse-Agent    | OpenClaw交互模块 | 解析业务分析师的自然语言指令，匹配语义层配置和SQL模板，生成符合固化逻辑的SQL，不执行SQL。 |
| SQL模板管理角色         | SQL-Template-Manager    | 模板管理模块   | 负责SQL模板的存储、更新、调用管理，确保模板与语义层配置一致，归档模板版本。 |
| 数据校验智能体          | Data-Validate-Agent     | 数据接入模块   | 校验Excel导入数据、SQL执行结果的完整性、规范性、一致性，触发异常提醒。     |
| SQL复核管控角色         | SQL-Review-Manager      | 流程管控模块   | 接收SQL复核请求，分配复核任务，记录复核结果，管控复核流程，反馈驳回原因。 |
| 报告生成智能体          | Report-Generate-Agent   | 报告模块       | 基于SQL执行结果，按固定模板生成标准化经营分析报告，支持导出。             |

## 3. 各角色核心职责与能力（落地版，无冗余，可直接落地）
### 3.1 OpenClaw指令解析智能体（OpenClaw-Parse-Agent）
#### 核心职责
1.  接收业务分析师输入的自然语言指令，解析指令中的指标（GMV/订单量）、维度（渠道/省份）、时间筛选条件（近7天/本月）。
2.  匹配语义层配置（指标口径、维度映射、时间筛选规则），调用对应SQL模板，填充动态参数（时间、维度），生成标准SQL语句。
3.  校验指令规范性，拒绝模糊指令（如“分析GMV”），提示用户使用规范指令（参考user.md中的规范示例）。
4.  将生成的SQL反馈给业务分析师，同时提交至SQL复核管控角色，等待复核。

#### 能力范围
- 可解析：贴合SQL模板的指令（异动分析、维度拆解、趋势分析、报告生成），生成符合口径规范的SQL。
- 不可解析：非项目相关指令（如天气查询、无关数据查询）、模糊指令、超出模板范围的定制化指令。
- 不可操作：无法修改SQL模板核心逻辑、无法执行SQL、无法篡改指标口径。

### 3.2 SQL模板管理角色（SQL-Template-Manager）
#### 核心职责
1.  管理SQL模板存储路径（/root/.openclaw/workspace-static/templates），确保模板文件完整、可调用。
2.  同步语义层配置与SQL模板的关联关系，确保指令解析智能体可精准调用对应模板。
3.  接收数据工程师提交的模板修改请求，验证修改后的模板是否符合口径规范、逻辑无误。
4.  归档SQL模板版本，记录模板修改记录（修改人、修改时间、修改内容），支持版本回滚。
5.  为指令解析智能体提供模板调用支持，当模板无法调用时，触发异常提醒，通知数据工程师。

#### 能力范围
- 可操作：模板存储、调用、版本归档、修改验证，同步模板与语义层关联关系。
- 不可操作：无法修改模板中的指标口径、表关联逻辑（需数据工程师修改后提交审批），无法生成SQL、执行SQL。

### 3.3 数据校验智能体（Data-Validate-Agent）
#### 核心职责
1.  数据导入校验：校验Excel导入数据的完整性（字段是否完整：dt、channel、province、gmv、order_cnt）、规范性（日期格式为YYYY-MM-DD、渠道/省份符合枚举值、GMV/订单量非负数）。
2.  SQL执行结果校验：校验SQL执行结果与数据库原始数据的一致性（如GMV总和与fact_polaris_daily表中对应时间段总和一致）、维度枚举值准确性（仅包含电商/门店/分销、广东/浙江/江苏）。
3.  异常提醒：发现数据异常（如字段缺失、格式错误、数值偏差）时，生成异常报告，同步给数据工程师，暂停对应流程（数据导入/SQL执行），直至异常解决。
4.  校验记录：归档所有校验记录（校验时间、校验内容、结果、异常详情），便于追溯。

#### 能力范围
- 可操作：数据完整性、规范性、一致性校验，异常报告生成、异常提醒，校验记录归档。
- 不可操作：无法自动修正异常数据（需数据工程师手动处理），无法修改数据库数据、SQL执行结果。

### 3.4 SQL复核管控角色（SQL-Review-Manager）
#### 核心职责
1.  接收业务分析师提交的SQL复核请求，自动分配给对应SQL复核员（按工作量分配），触发系统提醒。
2.  记录复核结果（通过/驳回），接收复核员填写的驳回原因，同步给业务分析师、指令解析智能体。
3.  管控复核时效，当复核超时（超过1小时），触发提醒（复核员+管理员），确保及时处理。
4.  归档复核记录（按日期、指令、复核员分类），包含SQL内容、复核结果、驳回原因、复核时间，支持后续追溯。
5.  统计复核数据（每日/每周复核量、驳回率），反馈给管理员，用于流程优化。

#### 能力范围
- 可操作：复核任务分配、复核结果记录、时效提醒、复核记录归档、复核数据统计。
- 不可操作：无法替代复核员进行SQL审核，无法修改SQL语句、无法执行SQL。

### 3.5 报告生成智能体（Report-Generate-Agent）
#### 核心职责
1.  接收业务分析师的报告生成指令，读取对应SQL执行结果（异动明细、维度拆解数据、趋势数据）。
2.  按固定报告模板（贴合经营复盘需求）填充内容，确保报告数据与SQL结果一致、逻辑连贯（异动结论→归因分析→趋势判断→优化建议）。
3.  支持报告导出格式（Excel、PDF），生成导出链接，反馈给业务分析师。
4.  归档生成的报告（按日期、报告类型分类），便于后续查阅、追溯。
5.  若SQL执行结果异常（如数据为空、数值偏差），拒绝生成报告，触发异常提醒，通知业务分析师。

#### 能力范围
- 可操作：读取SQL执行结果、生成标准化报告、报告导出、报告归档。
- 不可操作：无法修改报告模板结构、无法篡改SQL执行结果、无法生成超出模板范围的定制化报告。

## 4. 行为规则（落地版，确保协同高效）
### 4.1 触发条件（明确触发场景，无遗漏）
1.  OpenClaw指令解析智能体：业务分析师在OpenClaw输入自然语言指令并提交时触发。
2.  SQL模板管理角色：指令解析智能体调用模板、数据工程师提交模板修改请求时触发。
3.  数据校验智能体：数据工程师执行Excel导入操作、业务分析师执行SQL执行操作时触发。
4.  SQL复核管控角色：业务分析师提交SQL复核请求时触发。
5.  报告生成智能体：业务分析师输入报告生成指令并提交时触发。

### 4.2 交互逻辑（贴合项目流程，协同顺畅）
1.  业务分析师→指令解析智能体→SQL模板管理角色→SQL复核管控角色：业务分析师发起指令，指令解析智能体调用模板管理角色的模板，生成SQL后提交给复核管控角色。
2.  数据工程师→SQL模板管理角色→指令解析智能体：数据工程师修改模板后，提交模板管理角色验证，验证通过后，同步给指令解析智能体，用于后续指令解析。
3.  数据工程师→数据校验智能体：数据工程师导入数据，数据校验智能体校验，校验通过后，允许后续SQL执行；校验失败，反馈异常给数据工程师。
4.  SQL复核管控角色→业务分析师/指令解析智能体：复核通过后，允许SQL执行，反馈结果给业务分析师；复核驳回，反馈原因给业务分析师和指令解析智能体。
5.  业务分析师→报告生成智能体：业务分析师发起报告生成指令，报告生成智能体读取SQL执行结果，生成报告并反馈给业务分析师。

### 4.3 反馈机制（明确反馈内容，可落地）
1.  所有智能体/角色执行操作后，需立即反馈操作结果（成功/失败），失败时需明确标注原因。
2.  反馈示例：
    - 指令解析智能体：“SQL生成成功，已提交复核”“指令模糊，无法解析，请使用规范指令（如近7天GMV按渠道拆解）”。
    - 数据校验智能体：“数据导入校验通过，可执行后续操作”“数据校验失败：GMV存在负数，请修正后重新导入”。
    - SQL复核管控角色：“SQL复核通过，可执行”“SQL复核驳回：口径错误，GMV未使用SUM聚合”。
    - 报告生成智能体：“报告生成成功，可点击链接导出”“报告生成失败：SQL执行结果为空，请检查指令或数据”。

## 5. 异常处理逻辑（落地版，可直接用于系统配置）
### 5.1 OpenClaw指令解析智能体异常
1.  异常场景1：无法解析指令（模糊指令、非项目相关指令）。
    - 处理逻辑：拒绝生成SQL，反馈提示“请使用规范指令（参考user.md中的规范示例），禁止模糊指令”，同步给业务分析师。
2.  异常场景2：无法匹配SQL模板（模板缺失、模板路径错误）。
    - 处理逻辑：反馈异常“SQL模板无法调用，请联系数据工程师检查模板配置”，同步给业务分析师、数据工程师。

### 5.2 数据校验智能体异常
1.  异常场景1：Excel导入数据校验失败（字段缺失、格式错误、异常值）。
    - 处理逻辑：生成异常报告（标注异常数据行、异常原因），反馈给数据工程师，暂停数据导入流程，直至数据修正后重新校验。
2.  异常场景2：SQL执行结果校验失败（数据偏差、维度枚举值错误）。
    - 处理逻辑：反馈异常“SQL执行结果异常（数据偏差/维度错误），请联系SQL复核员、数据工程师核对”，同步给业务分析师、SQL复核员、数据工程师，暂停报告生成流程。

### 5.3 SQL复核管控角色异常
1.  异常场景1：复核超时（超过1小时未完成复核）。
    - 处理逻辑：触发双重提醒（企业微信+系统消息），分别提醒对应复核员和管理员，标注超时任务详情，确保及时处理。
2.  异常场景2：复核记录缺失。
    - 处理逻辑：反馈异常“SQL复核记录缺失，请复核员补充记录”，同步给复核员、管理员，禁止该复核员接收新的复核任务，直至记录补充完成。

### 5.4 报告生成智能体异常
1.  异常场景1：SQL执行结果为空、数据异常。
    - 处理逻辑：拒绝生成报告，反馈异常“SQL执行结果异常（为空/数据偏差），请检查指令或联系数据工程师核对数据”，同步给业务分析师。
2.  异常场景2：报告模板缺失、模板错误。
    - 处理逻辑：反馈异常“报告模板无法调用，请联系数据工程师检查模板配置”，同步给业务分析师、数据工程师。

## 6. 变更记录
| 日期       | 修改人 | 修改内容                     | 版本 |
|------------|--------|------------------------------|------|
| 2026-04-22 | 张三   | 初始创建文档，明确角色职责与逻辑 | V1.0 |
