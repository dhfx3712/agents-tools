# user.md（北极星指标OpenClaw数据分析项目 - 用户说明文档）
## 1. 文档说明
本文档明确项目所有用户的分类、画像、权限、交互流程及操作规范，所有用户需严格遵循，确保项目运行合规、数据安全、操作规范，适配项目“数据接入→分析→复核→报告”的全流程。

## 2. 用户分类及画像（落地版，明确职责）
### 2.1 业务分析师（核心用户，占比60%）
- 所属部门：业务运营部、市场部、销售部
- 用户属性：熟悉业务指标（GMV、订单量），无SQL编写、数据库操作能力，具备基础的数据分析思维。
- 核心需求：通过OpenClaw自然语言交互，快速获取指标异动、维度拆解、趋势分析结果，生成经营报告，用于日常监控、复盘汇报。
- 使用频率：每日1-2次（日常监控）、每周1次（周复盘）、每月1次（月复盘）。
- 使用场景：查询近7/30天GMV趋势、排查指标异动原因、按渠道/省份拆解指标贡献、生成复盘报告。

### 2.2 数据工程师（运维用户，占比20%）
- 所属部门：技术部
- 用户属性：具备MySQL数据库操作、SQL编写、OpenClaw配置能力，熟悉数据导入工具（DataX）。
- 核心需求：维护项目环境、处理Excel数据导入、更新SQL模板、配置OpenClaw语义层、排查数据/系统异常。
- 使用频率：每日1次（数据导入、异常排查）、每周1次（模板维护、环境检查）。
- 使用场景：Excel数据标准化处理、DataX导入数据库、修改SQL模板、配置.env及语义层YAML、处理数据校验异常。

### 2.3 SQL复核员（管控用户，占比15%）
- 所属部门：数据分析组
- 用户属性：具备SQL审核、指标口径把控能力，熟悉项目SQL模板固化逻辑。
- 核心需求：复核OpenClaw生成的SQL，确保表名、字段、口径、逻辑无误，记录复核结果，驳回不符合规范的SQL。
- 使用频率：每日3-5次（根据业务分析师需求）。
- 使用场景：接收SQL复核请求、核对SQL逻辑、标记复核结果、反馈驳回原因、归档复核记录。

### 2.4 管理员（超级用户，占比5%）
- 所属部门：数据分析组（项目负责人）
- 用户属性：具备项目整体管控能力，熟悉所有模块操作，掌握用户权限分配、审批流程。
- 核心需求：管理用户权限、审批SQL模板/指标口径变更、监控项目运行状态、处理重大异常、协调跨部门需求。
- 使用频率：每周1次（权限检查、状态监控）、按需处理审批/异常。
- 使用场景：创建/注销用户、分配用户权限、审批模板变更、处理重大数据异常、协调业务与技术需求。

## 3. 用户权限明细（落地版，无冗余，可直接用于权限分配）
| 用户类型       | 核心权限                                                                 | 禁止权限                                                                 |
|----------------|--------------------------------------------------------------------------|--------------------------------------------------------------------------|
| 业务分析师     | 1. OpenClaw自然语言交互（发起分析指令）；2. 查看SQL生成结果；3. 查看分析报告；4. 导出报告 | 1. 数据库操作（建表、导入、修改）；2. SQL模板修改；3. OpenClaw配置修改；4. SQL执行、复核 |
| 数据工程师     | 1. 数据库操作（建表、数据导入、查询、修改）；2. SQL模板修改、归档；3. OpenClaw配置（.env、语义层）；4. 数据校验、异常排查 | 1. 用户管理（创建、权限分配）；2. SQL复核、审批；3. 报告生成、导出（仅用于测试） |
| SQL复核员      | 1. 查看OpenClaw生成的SQL；2. 标记复核结果（通过/驳回）；3. 提交异常SQL；4. 归档复核记录 | 1. SQL执行、修改；2. 模板修改、OpenClaw配置；3. 数据库操作；4. 用户管理 |
| 管理员         | 所有权限（含上述所有用户权限）                                           | 无禁止权限，需对所有操作负责，留存操作记录                               |

## 4. 用户交互流程（落地版，可直接用于用户培训）
### 4.1 业务分析师核心流程（1-5分钟完成）
1.  登录OpenClaw系统，进入“自然语言交互”页面；
2.  输入规范指令（参考模板适配指令，如“近7天GMV按渠道拆解”“2026-04-22 GMV异动归因”）；
3.  查看OpenClaw生成的SQL，确认指令匹配后，提交SQL复核请求；
4.  等待SQL复核通过（1小时内），查看SQL执行结果；
5.  发起报告生成指令（如“基于当前结果生成经营分析报告”），导出报告（Excel/PDF）。

### 4.2 数据工程师核心流程（每日必做）
1.  接收业务部门提交的标准化Excel数据，校验数据完整性（字段、格式、异常值）；
2.  通过DataX工具将Excel数据导入MySQL数据库（fact_polaris_daily、dim_channel）；
3.  执行数据校验SQL，确认导入数据与Excel一致，无缺失、无偏差；
4.  检查OpenClaw环境、SQL模板、语义层配置，处理异常（如模板失效、配置错误）；
5.  归档Excel原始数据、导入记录，留存备查。

### 4.3 SQL复核员核心流程（即时处理）
1.  接收业务分析师提交的SQL复核请求（系统提醒）；
2.  核对SQL核心要点（表名是否正确、指标口径是否符合规范、维度枚举值是否正确、逻辑是否无误）；
3.  标记复核结果：通过（允许执行SQL）、驳回（注明驳回原因，如“口径错误，GMV未用SUM聚合”）；
4.  将复核结果同步给业务分析师，归档复核记录（按日期、指令分类）。

### 4.4 管理员核心流程（每周必做）
1.  登录系统，查看用户权限分配情况，删除无效账号，调整权限；
2.  审批数据工程师提交的SQL模板、指标口径变更请求，确认无误后批准；
3.  监控项目运行状态（数据导入成功率、SQL复核时效、异常数量）；
4.  处理重大异常（如数据库连接失败、模板批量失效），协调跨部门解决；
5.  检查文档更新情况，确保IDENTITY.md、user.md、AGENTS.md同步更新。

## 5. 用户操作规范（落地版，严格执行，避免风险）
1.  账号管理：所有用户需使用个人专属账号登录，禁止共用账号、转借账号；账号密码需定期更换（每月1次），禁止泄露。
2.  指令规范：业务分析师发起指令时，需使用规范表述，禁止模糊指令（如禁止“分析GMV”，需写“近7天GMV按日统计”）；参考附录中的规范指令示例。
3.  模板修改规范：数据工程师修改SQL模板后，需先测试模板可用性，确认符合口径规范，再提交管理员审批，审批通过后更新归档，同步修改语义层配置。
4.  SQL复核规范：SQL复核员需在收到请求后1小时内完成复核，驳回SQL时需明确说明原因，便于业务分析师、数据工程师修正；复核记录需完整，包含复核人、复核时间、结果、原因。
5.  数据安全规范：禁止私自导出、泄露数据库原始数据、SQL模板、分析报告；禁止修改数据库数据、SQL执行结果；操作后需留存操作记录，便于追溯。
6.  异常处理规范：所有用户遇到异常（如指令无法解析、SQL执行失败、数据校验异常），需及时反馈给对应负责人（业务分析师→SQL复核员/数据工程师；数据工程师→管理员），禁止私自处理。

## 6. 规范指令示例（业务分析师必看）
- 异动分析：“近7天GMV按日统计，判断是否有环比超±10%的异动”
- 维度拆解：“2026-04-22 GMV按渠道+省份拆解，分析各维度贡献度”
- 趋势分析：“近30天GMV按渠道趋势分析，展示每日数据”
- 报告生成：“基于近7天GMV异动结果，生成经营分析报告”

## 7. 异常反馈渠道（落地版，明确对接人）
| 异常类型       | 反馈对象       | 反馈方式       | 处理时效 |
|----------------|----------------|----------------|----------|
| 指令无法解析   | 数据工程师     | 企业微信私信   | 2小时内  |
| SQL复核驳回    | 业务分析师→数据工程师 | 复核备注反馈   | 1小时内  |
| 数据导入失败   | 数据工程师     | 企业微信私信   | 2小时内  |
| 系统无法登录   | 运维组（李四） | 企业微信私信   | 1小时内  |
| 重大数据异常   | 管理员         | 企业微信+电话  | 30分钟内 |

## 8. 变更记录
| 日期       | 修改人 | 修改内容                     | 版本 |
|------------|--------|------------------------------|------|
| 2026-04-22 | 张三   | 初始创建文档，明确用户权限与流程 | V1.0 |
